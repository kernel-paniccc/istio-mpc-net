# Source from k0s-vagrant-cluster: source env/local.env
# Keep all per-cluster state outside the source directories.
export VAGRANT_HOME="$PWD/env/runtime/vagrant-home"
export VAGRANT_DOTFILE_PATH="$PWD/env/runtime/vagrant"
export KUBECONFIG="$PWD/env/runtime/kube/config"
